spring:
  datasource:
    url: jdbc:h2:mem:arkitik_flotale
    username:
    password:
    driver-class-name: org.h2.Driver
  main:
    allow-bean-definition-overriding: true
  web:
    resources:
      add-mappings: false
logging:
  level:
    io:
      arkitik: debug
